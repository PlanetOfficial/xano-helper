const {StreamChat} = require('stream-chat');
const {getstream} = require('../constants')

/**
 * creates a new getstream channel
 * @param {string[]} user_ids 
 * @returns {string} channel_id
 */
const createChannel = (user_ids) => {

}

module.exports = {};